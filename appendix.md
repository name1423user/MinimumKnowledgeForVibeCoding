# 付録　あれば便利な知識

[← 目次に戻る](README.md)

必須ではないが、知っていると詰まる回数が減る知識。

## 環境構築の基礎

「自分のPCでは動くのに他の環境で動かない」の9割はここが原因。

- **仮想環境（venvなど）**: プロジェクトごとに使うライブラリのバージョンを分離する仕組み。分離せずグローバルにインストールすると、別プロジェクトのライブラリ更新が今のプロジェクトを壊す
- **Docker超入門**: 「OS・ライブラリのバージョンごと箱に固めて配る」仕組み。`Dockerfile`に書かれた手順通りに毎回同じ環境が再現されるため、「自分の環境では動く」を過去のものにできる
- **環境変数**: `os.environ["API_KEY"]`のように、コードの外側から値を注入する仕組み（[7.6節「秘密情報」](chapters/07-security.md#76-秘密情報)参照）。`.env`ファイルに書いた変数は、それを読み込む仕組み（`python-dotenv`など）を経由しない限り自動では反映されない、という点でよく詰まる

## CLIツールに慣れる

AIが出したコマンドをコピペで実行するだけでなく、意味を理解しておくと事故を防げる。

```
grep -rn "TODO" src/          # ファイル群から文字列を検索する（-r: 再帰的, -n: 行番号付き）
curl -I https://example.com   # HTTPレスポンスのヘッダーだけを取得して疎通確認する
cat data.json | jq '.users[0].name'  # JSONから特定のフィールドだけを取り出す
```

特に`rm -rf`や`git push --force`のような**取り消せない・上書きする系のコマンド**は、AIが提案してきても意味を理解してから実行する。「よく分からないが動きそうだから実行する」を一番避けるべき場面がここになる。

## 正規表現の基礎

パターンでの文字列マッチング。読めないと「なんかこのパターンにマッチしない」で詰まる。

```
^\d{3}-\d{4}$   # 「123-4567」のような郵便番号形式に完全一致
.*\.log$        # 末尾が.logのファイル名
```

`.`（任意の1文字）や`*`（直前の繰り返し）のような特殊記号を、意図せず「そのままの文字」として書いてしまう（エスケープし忘れる）のが典型的なハマりどころ。AIに正規表現を書かせた後も、意図通りの範囲にしかマッチしないか、極端な入力（空文字列、非常に長い文字列）で試してから使う。

## 設定ファイルのフォーマット（JSON/YAML/TOML）

読み書きに慣れておくと、AIとの設定周りのやり取りが速くなる。YAMLは**インデントの深さそのものが構文の一部**であるため、タブとスペースが混在するだけで壊れる、という事故が起きやすい（[8.2節](chapters/08-testing-and-cicd.md#82-ci継続的インテグレーションとは)のCI設定例もYAML）。

## ドキュメントの読み方

公式ドキュメント優先。Stack Overflowや古い記事の情報が今も正しいとは限らない。特にAIが提示した情報は学習時点のバージョンに基づいていることがあるため（[1.5節](chapters/01-ai-writes-code.md#15-aiには限界がある)のハルシネーションの節参照）、迷ったら公式ドキュメントの該当バージョンのページで裏取りする。

## セマンティックバージョニング（`1.2.3`の意味）

`メジャー.マイナー.パッチ`の3つの数字にはそれぞれ意味がある。

| 位置 | 名前 | 意味 | 例 |
|---|---|---|---|
| 1番目 | メジャー | 互換性のない変更（既存の使い方が壊れる可能性） | `1.x` → `2.0.0` |
| 2番目 | マイナー | 後方互換な機能追加 | `1.2.x` → `1.3.0` |
| 3番目 | パッチ | 後方互換なバグ修正 | `1.2.3` → `1.2.4` |

依存ライブラリの指定で`^1.2.3`（マイナー・パッチの範囲内で自動更新を許可）のような記法を見た時、この意味を知っていると「なぜ急にライブラリが更新されて壊れたか」の見当がつく。メジャーバージョンが上がった時だけ、変更内容（Breaking Changes）を確認してから更新するのが安全。

## ライセンスの基礎（MIT/GPL/Apacheなど）

依存ライブラリのライセンスを無視すると公開時に事故る。

- **MIT/Apache-2.0**: 改変・商用利用ともにほぼ自由。出典表示程度の義務で済むことが多い
- **GPL系**: 「コピーレフト」と呼ばれる性質を持ち、GPLのコードを組み込んで配布したプロダクトは、自作コード側にもソースコード公開義務が波及することがある

AIにライブラリの選定を任せる時、「ライセンスも確認して」と一言添えないと、機能面だけで選ばれてしまう。特に商用プロダクトにGPL系のライブラリを気づかず組み込んでしまうケースは実際に起きている。

## その他

- **README/コミットメッセージの書き方** — 未来の自分とAIへの引き継ぎ資料になる。「何を」だけでなく「なぜ」を書くと、AIに文脈を説明する手間が減る（[2.8節](chapters/02-reading-code.md#28-git)も参照）
- **ローカルと本番の違い** — OS差、パスの違い、環境変数の有無で「なぜかここだけ動かない」が起きる
- **エディタ/ターミナルの効率化** — ショートカットや補完に慣れるだけで、AIとの往復回数自体が減る

AIツールそのものの使い分け（チャット型・IDE統合型・CLI/エージェント型・自律実行型）は、[第9章](chapters/09-choosing-ai-tools.md)で扱っている。
